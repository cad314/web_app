<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>
    <body>
        <h1><%= title %></h1>
        <p>Hello <%= user.first%> <%= user.last%>! Welcome to your profile page. To go back to the home page,
            <a href="/">click here</a>.</p>

        <div class = "container">
            <h1> <a><%= user.first%></a> <a><%= user.last%></a> </h1>
            <h3><td><a><%= user.number%></a></td>
                <td><a><%= user.email%></a></td>
                <td><a><%= user.extraData.office %></a></td></h3>
        </div>


    </body>
</html>